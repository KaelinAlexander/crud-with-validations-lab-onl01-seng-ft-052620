<h1>Songs:</h1>
<% @songs.each do |song| %>
<div class="song">
  <%= link_to song.title, song_path(song) %>
  <%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %>
</div>
<% end %>
